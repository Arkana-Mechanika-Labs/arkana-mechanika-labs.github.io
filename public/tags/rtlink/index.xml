<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Darklands Restoration Project – Rtlink</title><link>https://arkana-mechanika-labs.github.io/tags/rtlink/</link><description>Recent content in Rtlink on Darklands Restoration Project</description><generator>Hugo -- gohugo.io</generator><language>en-us</language><lastBuildDate>Sun, 15 Mar 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://arkana-mechanika-labs.github.io/tags/rtlink/index.xml" rel="self" type="application/rss+xml"/><item><title>Devlog #002 — Phase 1: Naming the Map</title><link>https://arkana-mechanika-labs.github.io/posts/002-phase1-naming-the-map/</link><pubDate>Sun, 15 Mar 2026 00:00:00 +0000</pubDate><guid>https://arkana-mechanika-labs.github.io/posts/002-phase1-naming-the-map/</guid><description>
&lt;p&gt;Before you can understand what a piece of code does, you need to know what it&amp;rsquo;s called. That sounds obvious, but with a 1992 DOS executable compiled by Borland C with no debug symbols, every function arrives nameless. Ghidra calls them things like &lt;code&gt;FUN_16a1_1d2a&lt;/code&gt;. There are 388 of them.&lt;/p&gt;
&lt;p&gt;Phase 1 of the analysis was simple in concept: name every function. Figure out what it does and give it a name that means something. Do that for all 388, and you have a map of the entire codebase — not a detailed map, but a complete one. You know what exists and roughly where.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;How The Executable Is Structured&lt;span class="hx:absolute hx:-mt-20" id="how-the-executable-is-structured"&gt;&lt;/span&gt;
&lt;a href="#how-the-executable-is-structured" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;Darklands uses a Borland linker called &lt;strong&gt;RTLink&lt;/strong&gt; to manage code overlays. In 1992, programs that were too big to fit in the 640KB DOS memory limit would split their code into &amp;ldquo;overlays&amp;rdquo; — chunks that got loaded into memory on demand and swapped out when something else was needed. Darklands does this extensively.&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;.EXE&lt;/code&gt; file is 1.67 MB. The MZ header (the standard DOS executable format) only accounts for 174 KB of that. The remaining &lt;strong&gt;1.5 MB&lt;/strong&gt; is the overlay region — code segments that get loaded at runtime as the game needs them. It&amp;rsquo;s all packed into one file, appended after the main executable image.&lt;/p&gt;
&lt;p&gt;Ghidra can&amp;rsquo;t load this automatically. Getting it set up to see all 14 code segments required some custom scripting. Once it was working, the analysis could begin.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;The RTLink Overlay Manager&lt;span class="hx:absolute hx:-mt-20" id="the-rtlink-overlay-manager"&gt;&lt;/span&gt;
&lt;a href="#the-rtlink-overlay-manager" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;The first thing the agent found was the overlay manager itself — the code responsible for loading and unloading those 1.5 MB of overlays. This turned out to be surprisingly well-preserved: the key functions are in segment &lt;code&gt;19C0h&lt;/code&gt;, and once you find the loader at &lt;code&gt;0x19C0:021C&lt;/code&gt;, the rest unravels fairly cleanly.&lt;/p&gt;
&lt;p&gt;RTLink keeps a descriptor table at &lt;code&gt;19C0:0B7A&lt;/code&gt; where each 18-byte entry describes an overlay: its location in the file, its size, its memory address when loaded. The loader reads that table, seeks to the right position in the EXE, loads the bytes, applies relocations, and jumps. Standard stuff for 1992, but it was satisfying to confirm it from the code rather than assuming.&lt;/p&gt;
&lt;p&gt;It also supports &lt;strong&gt;EMS&lt;/strong&gt; (Expanded Memory Specification) — an old technique for accessing memory beyond the 640KB limit using a bank-switching trick. If EMS is available, the overlay manager will cache loaded overlays there rather than re-reading them from disk. That&amp;rsquo;s why Darklands ran better if you had an EMS driver installed.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;The Borland CRT&lt;span class="hx:absolute hx:-mt-20" id="the-borland-crt"&gt;&lt;/span&gt;
&lt;a href="#the-borland-crt" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;A significant chunk of the 388 functions turned out to be Borland&amp;rsquo;s C runtime library — standard functions like &lt;code&gt;fopen&lt;/code&gt;, &lt;code&gt;fread&lt;/code&gt;, &lt;code&gt;fwrite&lt;/code&gt;, &lt;code&gt;malloc&lt;/code&gt;, &lt;code&gt;sprintf&lt;/code&gt;, and so on. These aren&amp;rsquo;t game logic; they&amp;rsquo;re the plumbing. The agent recognized them quickly by their patterns and named them with &lt;code&gt;crt_&lt;/code&gt; prefixes.&lt;/p&gt;
&lt;p&gt;Finding the CRT functions was actually useful — once you know which functions handle file I/O, you can follow their call chains to find the game&amp;rsquo;s actual file loading code.&lt;/p&gt;
&lt;hr&gt;
&lt;h2&gt;Key Subsystems Found&lt;span class="hx:absolute hx:-mt-20" id="key-subsystems-found"&gt;&lt;/span&gt;
&lt;a href="#key-subsystems-found" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;By the end of Phase 1, the map looked like this:&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Graphics pipeline&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;LZW sprite decoder (segment &lt;code&gt;147Ch&lt;/code&gt;) — decompresses sprite data&lt;/li&gt;
&lt;li&gt;VGA renderer + sprite cache/blit (segments &lt;code&gt;147Ch&lt;/code&gt;, &lt;code&gt;13ECh&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;LZSS decompressor (segment &lt;code&gt;15D9h&lt;/code&gt;) — used for other compressed data&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Resource system&lt;/strong&gt; (segment &lt;code&gt;1617h&lt;/code&gt;)&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;resource_table_lookup&lt;/code&gt; — linear scan of a catalog&amp;rsquo;s flat directory&lt;/li&gt;
&lt;li&gt;&lt;code&gt;resource_open&lt;/code&gt;, &lt;code&gt;resource_read_at&lt;/code&gt;, &lt;code&gt;resource_read_chunk&lt;/code&gt; — the streaming read pipeline&lt;/li&gt;
&lt;li&gt;&lt;code&gt;resource_fatal_error&lt;/code&gt; — log error code 3 and exit(99). Cheerful.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Game loop&lt;/strong&gt; (address &lt;code&gt;0x13A1A&lt;/code&gt;)&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;A state machine with 99 states (0 through 0x62)&lt;/li&gt;
&lt;li&gt;Dispatches via a function pointer table: &lt;code&gt;(**(code**)(state × 4))(segment)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Initial state on new game: &lt;code&gt;0x29&lt;/code&gt;. Terminal state: &lt;code&gt;0x62&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Each state handler is a separate function — travel map, city, combat, rest, and so on&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Calendar&lt;/strong&gt; (address &lt;code&gt;0x18CB6&lt;/code&gt;)&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;game_get_current_date&lt;/code&gt; — converts the DOS system clock into the game&amp;rsquo;s medieval calendar&lt;/li&gt;
&lt;li&gt;The in-game date is real: time passes whether you&amp;rsquo;re doing anything or not&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;RNG&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Linear congruential generator: &lt;code&gt;seed × 0x343FD + 0x269EC3&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Seed at addresses &lt;code&gt;0x7B20&lt;/code&gt;/&lt;code&gt;0x7B22&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Used everywhere randomness is needed: combat rolls, encounters, loot&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2&gt;The Result&lt;span class="hx:absolute hx:-mt-20" id="the-result"&gt;&lt;/span&gt;
&lt;a href="#the-result" class="subheading-anchor" aria-label="Permalink for this section"&gt;&lt;/a&gt;&lt;/h2&gt;&lt;p&gt;382 of 388 functions named. The remaining 6 are in segments that haven&amp;rsquo;t been fully analyzed yet — likely minor utility functions.&lt;/p&gt;
&lt;p&gt;More importantly: the map exists. We know the shape of the codebase. We know which segment handles rendering, which handles resources, which handles the game loop. Phase 2 can now go deep into each of those areas and actually understand what they&amp;rsquo;re doing.&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;em&gt;Next: The Knowledge Base — community research that was already waiting for us.&lt;/em&gt;&lt;/p&gt;</description></item></channel></rss>